<div class="container">
  <h2>Datos del Conductor</h2>

  <form [formGroup]="driverForm" (ngSubmit)="onSubmit()">
    <label>
      Nombre: 
      <input type="text" formControlName="FirstName" required>
    </label>

    <label>
      Apellido: 
      <input type="text" formControlName="LastName" required>
    </label>

    <label>
      Género:
      <select formControlName="Gender">
        <option value="male">Masculino</option>
        <option value="female">Femenino</option>
      </select>
    </label>

    <label>
      Teléfono: 
      <input type="text" formControlName="Phone" required>
    </label>

    <label>
      Email: 
      <input type="email" formControlName="Email" required>
    </label>

    <label>
      Nacionalidad:
      <input type="text" 
       matInput
       formControlName="Nationality" 
       (input)="handleNationalityInput($event)"
       [matAutocomplete]="auto">
      <mat-autocomplete #auto="matAutocomplete" (optionSelected)="onCountrySelected($event.option.value)">
        <mat-option *ngFor="let country of filteredCountries" [value]="country">
          {{country.label}}
        </mat-option>
      </mat-autocomplete>
      <input type="hidden" formControlName="NationalityISO">
    </label>

    <label>
      Documento:
      <select formControlName="ConfigurationDocumentId">
        <option *ngFor="let doc of validDocuments" [value]="doc.Id">
          {{doc.Name}}
        </option>
      </select>
    </label>

    <label>
      Documento Nro: 
      <input type="text" formControlName="DocumentNumber" required>
    </label>

    <label>
      Fecha de nacimiento:
      <input type="date" 
             formControlName="DOB"
             [max]="maxDate | date:'yyyy-MM-dd'"
             [min]="minDate | date:'yyyy-MM-dd'">
    </label>

    <button type="submit" [disabled]="driverForm.invalid">Reservar</button>
  </form>
</div>